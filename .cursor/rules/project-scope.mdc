---
description: RSSAgent project scope — stack, skills, and LLM architecture boundaries
alwaysApply: true
---

# RSSAgent Scope

Sports-science RSS ingestion (Python) → SQLite dedupe → alerts + conditional LLM content generation + **Next.js/shadcn UI** (dev portal or auth-gated). Public git repo: never commit secrets, `.env`, or `*.db`.

## Stack

- **Ingestion:** `feedparser`, `pandas`, `sqlite3`, `schedule`, optional `BeautifulSoup` fallback
- **LLM:** Multi-provider router — **Gemini primary**; AWS Bedrock (Claude) secondary
- **UI:** Next.js App Router, shadcn/ui, TypeScript (dev dashboard → optional auth portal)
- **Deploy:** Backend — VPS / Lambda / Workers; UI — Vercel (typical)

## Two paths

1. **Alert** — every new article → fast customized notification (do not block on LLM)
2. **Generate** — conditional synthesis + markdown output (per-article and digest); rules TBD

Outputs: `content/articles/`, `content/digests/` with YAML frontmatter (sources, model, provider).

## Skills to use

Prefer project skill `.cursor/skills/rssagent/SKILL.md` first. Then: Gemini/Vertex, Python deps, AWS Lambda/serverless, Cloudflare Workers/wrangler, **Next.js/shadcn/vercel-react-best-practices** for UI. See `docs/context-setup.md` for plugin pruning.

## Skills to ignore

Marketing, Figma/Stitch, BigQuery/dbt/Dataform, SageMaker/HyperPod, unrelated frameworks (Rails, WinUI, Chrome ext), media gen, Notion/Linear/Slack research, MCP plugin authoring, Cursor meta utilities.

## Code rules

- Env vars for all API keys and webhooks; document in `.env.example`
- Provider logic in one router module — not in the RSS poll loop
- `time.sleep` between feed requests; log errors, don't crash the cycle
- Match `src/rssagent/` logging patterns for new Python code
- Minimal diffs; no over-engineering until generation thresholds are defined
